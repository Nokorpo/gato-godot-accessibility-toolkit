## Random Timer

This is a custom Timer node that fires its timeout signal at a random interval.

You can set it up by creating a new instance and defining its `minimum_wait_time`
and `maximum_wait_time`. The parent Timer's `wait_time` property will be overwritten.
