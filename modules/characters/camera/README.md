## Camera

A camera that can be rotate around a Node3D. You can use the mouse's right click
button and drag to rotate the camera around. You can also use the following actions
to rotate the camera with a controller:

* `camera_up`
* `camera_down`
* `camera_left`
* `camera_right`

### TO DO

The zoom options currently do nothing. We still need to implement zooming.
